PROMPT_SKILL_META = """
# 角色

你是一位资深的 AI Agent Skill 评估专家。你的任务是根据 Skill 评估规范对用户提供的 Skill **元数据文件 (SKILL.md)** 进行评估。你必须严格遵循以下五个维度及其详细的评分标准进行评估。

---

## 1. 目的适配性 (1-5分)

评估 Skill 是否具有清晰的单一目的，并能让 LLM 准确识别调用时机。

### 评估要点

#### A. 职责边界清晰
- Skill 的所有功能是否服务于同一核心目标？
- 不跨越不相关的问题域
    - 正例: "数据库备份" - 包含备份执行、验证、存储 (一个完整的原子任务)
    - 反例: "数据库管理" - 包含备份、性能调优、用户权限管理、监控告警 (多个独立任务)
       
#### B. 触发识别
- `description` 需要包含可匹配的具体信号词或场景描述
    - 正例："当出现'端口被占用'、'进程无响应'或需要重启服务时使用"
    - 反例： "用于处理服务相关问题"

### 评分标准
**5 分 (优秀)**:
- Skill 聚焦于单一且清晰的目的，职责边界明确
- 其描述中包含了具体、可匹配的触发信号或场景描述，LLM 可准确判断调用时机

**3 分 (合格)**: 
- Skill 具有主要目标，但包含了可拆分或耦合的职责
- 触发条件较为抽象，LLM 需要结合上下文或经验才能判断

**1 分 (差)**: 
- Skill 目的不明确或跨越多个不相关任务
- Description 缺少明确触发条件

---

## 2. 结构规范性 (1-5分)

评估 Skill 的元数据规范、内容组织和信息密度。

### 评估要点

#### A. 元数据规范
- Name: 最大 64 字符，仅限小写字母、数字、连字符
- Description: 必须使用第三人称，长度 ≤1024 字符

#### B. 内容精炼
- 不解释通用概念或专家常识，只包含 LLM 训练数据中可能缺失的专有信息
- 可以包含简明扼要的用法示例和最佳实践

#### C. 渐进式披露
- SKILL.md 作为"目录"，保持在 500 行以内
- 详细内容（API 文档、复杂示例、历史记录）外置到独立文件

#### D. 工具声明（如适用）
- 如明确声明了可用工具，需要为完成任务的最小必要集
        
### 评分标准
**5 分 (优秀)**: 
- 元数据符合规范
- `SKILL.md` 内容高度精炼，如有需要能通过索引链接实现渐进式披露
- 内容假设 LLM 具备专家背景，无通用常识性描述
- 工具集（如有）无冗余

**3 分 (合格)**: 
- 元数据基本规范
- 结构清晰，但 `SKILL.md` 包含少量可拆分的背景说明或非核心代码块

**1 分 (差)**: 
- 元数据不规范
- 所有代码片段、长篇案例、API 文档均堆砌在单一的 `SKILL.md` 文件中，未做有效拆分
- 工具集冗余

---

## 3. 指令适配性 (1-5分)
评估指令自由度是否与任务的风险等级和确定性相匹配。

### 指令自由度分类
#### A. 低自由度（固定脚本）
- 适用于高风险，例如：数据库迁移、生产环境配置变更
- 适用于确定性流程，例如：标准化的部署流程、合规性检查
- 特点：提供完整的可执行脚本

#### B. 中自由度（参数化模板）
- 适用于半结构化任务，例如：日志分析、报告生成
- 适用于有首选方案但允许调整的任务，例如：代码重构、测试用例生成
- 特点: 提供推荐步骤和参数，LLM 可根据上下文调整

#### C. 高自由度（启发式指导）
- 适用于创造性任务，例如：文档撰写、代码审查
- 适用于依赖上下文判断的任务，例如：性能优化建议、架构设计评审
- 特点: 提供原则和检查清单，LLM 自主决策执行方式

### 评分标准
**5 分 (优秀)**：
- 指令自由度与任务风险完全匹配。

**3 分 (合格)**: 
- 指令自由度基本匹配，但在某些高风险缓解约束模糊，或在某些灵活任务中限制过死。

**1 分 (差)**: 
- 指令自由度与任务性质严重错配，如高风险操作让 LLM 自行判断。

---

## 4. 内容一致性 (1-5分)
评估 skill 在术语、表达风格是否保持一致，且不依赖隐含的时效性假设。

### 评估要点
#### A. 术语统一
- 同一语义概念在 Skill 中始终使用统一术语。也允许使用“抽象概念 → 具体实现”的稳定映射，同一层级术语混用才视为不一致。
    - 正例: 全文统一使用"工作进程" (Worker Process)
    - 反例: 混用"工作进程"、"Worker"、"子进程"

#### B. 表达一致
- 指令风格前后统一（如统一使用命令式或描述式）。

#### C. 时效性管理
- 不包含易于过时的硬编码信息（如日期、版本号）
- 例外：文档自身版本号、明确标注的"示例值"或"历史参考"
            
### 评分标准
**5 分 (优秀)**: 
- 对同一概念始终使用统一术语。
- 表达风格前后一致。
- 不包含易于过时的硬编码的时效性信息（或已明确标注）。

**3 分 (合格)**: 
- 整体可理解，但存在少量术语不统一或混用。
- 或包含少量的隐含时效性信息。

**1 分 (差)**: 
- 术语混乱，同义词混用，表达矛盾。
- 包含明显的时效性或硬编码信息。

---

## 5. 运维可靠性 (1-5分)
评估 Skill 的安全边界、灾难恢复和操作可观测性。

### 评估要点
#### A. 安全防护（所有运维类的 skill 必须）
- 对高危操作进行了严格的安全边界定义，明确被禁止的操作，对于高风险操作解释违规后果
- 是否采用了最小权限原则，避免赋予不必要的系统访问权限？

#### B. 灾难恢复（涉及状态变更的 skill 必须）
- 是否包含了完善的灾难恢复计划，包括备份策略、回滚机制与验证流程？

#### C. 可观测性（分类要求）
- 诊断类 skill: 提供问题识别方法和关键信息输出格式
- 修复类 skill: 提供操作验证步骤，和成功或失败的判断标准
- 数据处理类 skill: 提供结果可追溯性（如输出路径、记录数）
- 所有 skill: 禁止静默失败，操作失败时必须明确告知

#### D. 人机协作边界（如适用）
- 明确哪些操作需要人工确认
          
### 评分标准
**5 分 (优秀)**: 
- 明确定义禁止操作或违规后果。
- 根据 Skill 类型提供完整的可观测性保障。
- 所有高危操作有备份机制，所有状态变更操作均具备回滚机制。
- 禁止静默失败。
- 明确人工干预点 (如适用)。

**3 分 (合格)**: 
- 基本遵循安全与可靠性原则，但存在不完整之处（如仅部分操作有备份，可观测性保障不全面）。

**1 分 (差)**: 
- 缺乏明确的安全护栏。
- 状态变更操作无回滚保障。
- 缺少必要的可观测性保障。
- 存在静默失败风险。

---

## 任务指令

现在，请评估以下提供的 **Skill 元数据 (SKILL.md)** 内容。对于每个维度，请严格按照下面的 JSON 格式输出你的评分和理由。理由必须具体，并引用 Skill 中的原文作为论据。

**Skill 内容 (SKILL.md):**
```
{content}
```

## 输出格式

请严格按照以下 JSON 格式返回你的评估结果，不要使用 markdown 代码块包裹（即不要使用 ```json 或 ``` 标记），不要包含任何额外的解释或说明。

```json
{{
  "overall_comment": "对 Skill 的整体质量进行简要总结，并提出核心改进建议。",
  "detailed_evaluation": [
    {{
      "dimension": "职责与触发",
      "score": "在这里填写 1-5 的整数评分",
      "justification": "在这里填写具体的评估理由，必须引用 Skill 内容作为证据。"
    }},
    {{
      "dimension": "结构与效率",
      "score": "在这里填写 1-5 的整数评分",
      "justification": "在这里填写具体的评估理由，必须引用 Skill 内容作为证据。"
    }},
    {{
      "dimension": "指令粒度",
      "score": "在这里填写 1-5 的整数评分",
      "justification": "在这里填写具体的评估理由，必须引用 Skill 内容作为证据。"
    }},
    {{
      "dimension": "内容一致性",
      "score": "在这里填写 1-5 的整数评分",
      "justification": "在这里填写具体的评估理由，必须引用 Skill 内容作为证据。"
    }},
    {{
      "dimension": "风险管控",
      "score": "在这里填写 1-5 的整数评分",
      "justification": "在这里填写具体的评估理由，必须引用 Skill 内容作为证据。"
    }}
  ]
}}
```
"""

PROMPT_CODE_QUALITY = """
# Skill 代码质量评估提示词

## 角色

你是一位资深的 AI Agent Skill 评估专家，尤其擅长代码质量审查。你的任务是根据《Skill 开发与评估规范》中的“代码质量”维度，对用户提供的 **Skill 参考实现 (references)** 内容进行评估。

## 评估规范

你必须严格遵循以下“代码质量”维度的详细评分标准进行评估。

---

### 代码质量 (1-5分)
**核心要求**: 评估 Skill 关联的参考实现和脚本文件，强调其独立性、健壮性与自愈能力。
**评估要点**:
        *   是否独立实现业务逻辑？
        *   是否路径和依赖管理完善？
        *   是否包含具体的错误处理修复建议？
        *   是否关键步骤自带验证逻辑，无需 LLM 二次核对？
*   **5 分 (优秀)**: 达到工程级健壮性，闭环可靠。脚本完全独立地实现业务逻辑；路径和依赖管理完善；错误处理包含具体的修复建议；关键步骤自带验证逻辑，无需 LLM 进行二次核对。
*   **3 分 (合格)**: 逻辑通顺，但严谨性不足。脚本可正常执行，但存在少量平台特定的路径表示法或未解释的常量；错误处理仅抛出错误，未提供修复建议。
*   **1 分 (差)**: 逻辑外溢或环境脆弱。脚本功能不完整，将本应由其处理的逻辑交由 LLM “即兴发挥”；缺乏错误处理机制；路径硬编码导致在不同环境中无法运行；依赖包缺失。

---

## 任务指令

现在，请评估以下提供的 **Skill 参考实现 (references)** 内容。请严格按照下面的 JSON 格式输出你的评分和理由。理由必须具体，并引用 Skill 参考实现（references）中的原文作为论据。

**Skill 参考实现内容:**
```
{content}
```

## 输出格式

请严格按照以下 JSON 格式返回你的评估结果，不要使用 markdown 代码块包裹（即不要使用 ```json 或 ``` 标记），不要包含任何额外的解释或说明。

```json
{{
  "overall_comment": "在这里对代码的整体质量进行简要总结，并提出核心改进建议。",
  "detailed_evaluation": [
    {{
      "dimension": "代码质量",
      "score": "在这里填写 1-5 的整数评分",
      "justification": "在这里填写具体的评估理由，必须引用 Skill 内容作为证据。"
    }}
  ]
}}
```
"""
