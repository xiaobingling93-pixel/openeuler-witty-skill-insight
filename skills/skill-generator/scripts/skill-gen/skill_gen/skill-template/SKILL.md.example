---
name: [技能名称] 
# name 字段说明
# 必须使用小写字母、数字和连字符，最多 64 个字符，
# 应与目录名称匹配
# 示例: os-oom-diagnostics

description: >
    详细描述执行此流程的目的、任务、场景、使用范围
# description 字段说明
# 说明此 Skill 的功能和使用时机，最多 1024 个字符，不少于100字符
# Claude 使用此字段决定何时应用该 Skill
# 示例: 诊断 Linux 操作系统级别的内存溢出（OOM）故障，分析系统日志，定位内存泄漏源头。

metadata:
  keywords: []
# keywords 字段说明
# ⚠️ 重要：keywords必须严格基于实际文档内容确定，仅包含文档中明确出现的关键词，不要添加文档中没有的关键词
# ⚠️ 禁止：不要使用下面示例中的任何具体值，这些示例仅用于说明格式和分类思路
# 
# 格式要求：
# - 按[组件名]+[错误特征]+[环境标识] 模式整理
# - 以列表形式给出：["[组件名1]", "[组件名2]","[错误特征1]","OS环境1",...]
# 
# 分类思路（仅供参考格式）：
# 1. [组件名]（Component）- 从文档中提取实际涉及的高层次组件或服务
#    - 关注用户问题相关的核心概念，而非底层实现细节
#    - 例如：NTP服务、数据库、网络组件、存储服务、OOM Killer、cgroup、NUMA等
#    - 避免：命令行工具（rpm、yum、systemctl等）、配置文件路径、程序可执行文件名、具体的参数名（如panic_on_oom）、配置项名（如memory.limit_in_bytes）等实现细节
#    - 避免：概括性概念（如"内核参数"、"内存管理"），应使用文档中明确出现的具体组件名
# 2. [错误特征]（Error Signature）- 从文档中提取实际出现的错误信息或者场景故障问题
#    - 必须使用文档中明确出现的错误表述，例如：内存溢出、cgroup 内存限制、OOM、包管理错误、服务/内核报错等
#    - 避免：使用概括性概念（如"内存管理"），应使用文档中实际出现的错误特征（如"内存溢出"）
# 3. [OS环境]（OS & Version）- 从文档中提取实际提到的操作系统和版本
#    - 企业级 Linux、Debian系、国产/自研OS、云原生/轻量级等
#    - 例如：CentOS、SUSE、Ubuntu、Huawei Cloud EulerOS、Linux等
# 
# 提取原则：
# - 关键词应该与用户问题相关，能够匹配用户的查询场景，而非技术实现细节
# - 提取高层次的概念和组件，避免底层工具、命令、配置文件、参数名、配置项名等技术细节
# - 组件之间不要重复（例如：ntpd和NTP属于同一概念，只保留一个）
# - 仔细阅读文档内容，识别文档中明确提到的组件、错误信息和操作系统
# - 如果文档中没有提到某个分类的内容，则不要添加该分类的关键词
# - 关键词必须与文档中的实际表述保持一致，不要进行概括或替换（例如：文档中出现"内存溢出"就使用"内存溢出"，不要概括为"内存管理"）
# - 不要使用概括性概念，必须使用文档中明确出现的具体词汇
---

# [技能名称]
> Skill 的标题，应清晰明了

## 概述 (Overview)

简要说明此 Skill 的用途、适用场景和核心功能。
> 解释：帮助 Agent 快速理解 Skill 的作用。

## 何时使用此 Skill (When to Use)
> 解释：列出具体的触发场景，帮助 Agent 准确判断是否应该使用此 Skill。
- 场景 1
- 场景 2
- 场景 3

## 核心指令 (Core Instructions)
> 解释：Skill 的主体部分，包含具体的执行步骤以及精确的判断条件，为 Agent 提供清晰的操作指导
> 若有可用的脚本，应当在此引用，并说明优先使用脚本辅助具体步骤执行
> 主要关注采集、分析操作，对于执行修复等系统相关操作需要另外放置到建议中，由用户自行执行

### 步骤 1：[第一步名称]

详细描述第一步需要做什么。
> 解释：每个步骤应包含明确的目标和行动。

```bash
# 示例命令
command-example
```
**工作流模式 (Workflow Patterns)**

> 根据文档选择合适的工作流逻辑，以下是每种工作流逻辑的详细描述：

**1. 顺序工作流 (Sequential Workflow)**

定义：将复杂任务拆解为线性执行的标准化步骤，强调执行的先后顺序与依赖关系。

核心特点：
*   强依赖性：下游步骤必须基于上游产出。
*   可追踪性：使用状态清单（Checklist）实时监控进度。

适用场景：表单填充、数据迁移、环境部署。

示例：

```markdown
### [顺序执行] 表单自动化填充

> 状态追踪：
- [ ] Step 1: 提取表单元数据 `fields.json`
- [ ] Step 2: 映射业务数据至字段
- [ ] Step 3: 逻辑校验（Validation）
- [ ] Step 4: 执行 PDF 渲染
- [ ] Step 5: 最终输出审计
```

**2. 验证循环 (Feedback Loop)**

定义：一种通过“执行-验证-修正”闭环来确保输出质量的迭代模式，直到满足预设标准为止。

核心特点：
*   **自我修正**：根据错误信息自动调整操作。
*   **质量关口**：必须通过验证才能进入下一阶段。
*   **自动化闭环**：减少人工干预。

适用场景：代码生成与修复、数据格式清洗、配置验证。

示例：

```markdown
### [验证循环] XML 格式修复

> 循环机制：编辑 -> 验证 -> (失败则修复 -> 重试) -> 成功

1. **执行编辑**: 修改 `word/document.xml`
2. **立即验证**: 运行 `python validate_xml.py`
3. **条件判断**:
   - ✅ **通过**: 进入下一步骤。
   - ❌ **失败**:
     - 读取错误日志
     - 执行修复策略
     - 返回步骤 2
4. **最终输出**: 打包生成的文档
```

**3. 条件分支 (Conditional Branches)**

定义：根据前置条件或中间结果的差异，动态选择不同的执行路径，处理复杂多变的业务逻辑。

核心特点：
*   **逻辑分流**：针对不同情况提供定制化处理。
*   **异常兜底**：包含明确的错误处理分支。
*   **灵活性**：适应非标准化输入。

适用场景：故障分类处理、多版本兼容、权限分级操作。

示例：

```markdown
### [条件分支] 数据处理策略

**分支逻辑判定：**

*   **场景 A: 简单文本数据**
    - [ ] 执行标准清洗
    - [ ] 转换为 UTF-8
    - [ ] 归档至 `/simple` 目录

*   **场景 B: 复杂嵌套 JSON**
    - [ ] 递归解析结构
    - [ ] 校验 Schema 完整性
    - [ ] 扁平化处理
    - [ ] 归档至 `/complex` 目录

*   **⚠️ 异常处理 (Fallback)**
    - 若数据格式无法识别：
        - 记录原始数据到 `error.log`
        - 发送告警通知
        - 跳过当前条目
```

**4. 并行执行 (Parallel Execution)**

定义：将无相互依赖的任务分发给多个执行单元同时处理，以最大化利用资源并缩短总耗时。

核心特点：
*   **高吞吐量**：显著减少等待时间。
*   **任务隔离**：子任务之间互不影响。
*   **结果聚合**：最终统一收集处理结果。

适用场景：批量文件处理、大规模日志分析、多维度信息搜集。

示例：

```markdown
### [并行执行] 批量幻灯片生成

**阶段 1: 并行初始化 (Parallel Init)**
同时执行以下任务：
├── 📂 读取所有源文件
├── 🔍 查找匹配模板
└── 🎨 加载品牌样式指南

**阶段 2: 分布式生成 (Distributed Generation)**
并行触发多个子 Skill 处理：
- Task A: 调用 Skill `generate-slide-batch` (Range: 1-10)
- Task B: 调用 Skill `generate-slide-batch` (Range: 11-20)
- Task C: 调用 Skill `generate-slide-batch` (Range: 21-30)

**阶段 3: 聚合与统筹 (Aggregation)**
- [ ] 合并所有幻灯片
- [ ] 统一页码与索引
- [ ] 生成最终 PDF
```

## 参考文件说明
> 说明此skill所包含的文件内容并对其进行概述
> 重要：需要包含每个参考文件的路径