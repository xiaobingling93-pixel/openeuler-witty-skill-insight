你帮我生成一个基于安全故障类问题分析的skills，要求：
（1）你的操作对象是linux系统，优先兼容centos系列（包括enleros）
（2）我提供给你这个skills需要覆盖的安全场景，你参考如下需要都能覆盖到

【安全故障场景和故障模式说明】

| 安全类别          | 常见故障场景               | 可能原因                             | 异常表现             | 排查/诊断命令                                                        | 用户定位方法/思路                            | 运维诊断提问思路                                  |                                |
| ------------- | -------------------- | -------------------------------- | ---------------- | -------------------------------------------------------------- | ------------------------------------ | ----------------------------------------- | ------------------------------ |
| **用户认证/登录安全** | 用户无法登录               | PAM 配置错误、SSSD/LDAP 服务异常、账号锁定     | 登录失败、ssh 或本地登录报错 | `journalctl -u sshd`, `getent passwd`, `systemctl status sssd` | 查看登录日志、检查账户状态、验证认证服务是否可达             | “用户账号是否被锁定？认证服务是否可用？LDAP/SSSD 服务是否正常？”    |                                |
|               | 密码策略失效               | PAM 密码策略配置错误                     | 密码不符合策略要求，无法修改密码 | `cat /etc/pam.d/*`, `chage -l 用户`                              | 检查 PAM 配置及密码策略，尝试修改测试用户密码            | “密码策略配置是否正确？用户尝试修改密码是否符合策略？”              |                                |
|               | 账号权限异常               | 用户组或角色配置错误                       | 无法执行特定命令或访问资源    | `id 用户`, `groups 用户`                                           | 核对用户组和 sudo 权限，确认角色策略是否生效            | “用户属于正确的组吗？角色权限是否配置正确？”                   |                                |
| **权限控制**      | 文件或目录权限异常            | chmod/chown 配置错误、ACL/SELinux策略冲突 | 用户无法访问文件、进程无法启动  | `ls -l`, `getfacl`, `sestatus`                                 | 对比文件实际权限和策略文档，确认 SELinux/AppArmor 状态 | “文件或目录权限是否被误修改？SELinux/AppArmor 是否限制了访问？” |                                |
|               | 程序执行权限异常             | 权限位配置错误或策略限制                     | 关键服务无法启动或报错      | `ls -l /usr/bin/程序`, `getcap`                                  | 检查二进制文件权限及安全能力（capabilities）         | “程序文件是否有执行权限？是否有能力限制(cap)？”               |                                |
| **防火墙与网络访问**  | 网络访问被阻塞              | iptables/firewalld 配置错误、规则冲突     | 无法访问服务端口或主机      | `iptables -L -n -v`, `firewall-cmd --list-all`, `ss -tuln`     | 核对防火墙规则与服务端口，使用 telnet/nc 测试连通性      | “防火墙规则是否阻塞了流量？端口是否开放？网络策略是否正确？”           |                                |
|               | 异常端口暴露               | 防火墙未启用或策略不生效                     | 内网/外网端口暴露，增加攻击面  | 同上 + `nmap` 扫描                                                 | 扫描内外网端口，确认只开放必要端口                    | “是否有非必要端口暴露？防护策略是否生效？”                    |                                |
|               | 网络策略异常               | TCP/IP 参数错误或路由策略错误               | 网络连接不稳定或被拒绝      | `ip route`, `sysctl net.*`                                     | 检查路由和内核网络参数，确认策略一致性                  | “路由和网络策略是否正确？是否有策略冲突或错误配置？”               |                                |
| **系统审计/日志安全** | 日志无法记录               | rsyslog/auditd 服务异常或配置错误         | 安全事件无法追踪         | `systemctl status rsyslog`, `ausearch -m`                      | 检查服务状态、配置文件，验证日志写入权限和磁盘空间            | “日志服务是否启动？配置是否正确？磁盘空间是否足够？”               |                                |
|               | 日志被篡改或删除             | 权限不当、磁盘满                         | 审计日志不完整，无法定位问题   | `ls -l /var/log`, `auditctl -l`                                | 对比日志完整性，启用 auditd 审计关键目录             | “日志文件权限是否正确？日志是否被意外删除？磁盘是否满？”             |                                |
|               | 审计规则未生效              | auditd 配置错误或规则缺失                 | 未记录关键操作或访问事件     | `auditctl -l`, `ausearch`                                      | 核对规则文件，模拟操作确认是否被记录                   | “关键操作是否被审计？规则是否配置正确且已生效？”                 |                                |
| **安全策略与防护**   | SELinux/AppArmor策略失效 | 策略被修改或禁用                         | 服务异常、访问被拒或权限异常   | `getenforce`, `sestatus`                                       | 检查策略状态，核对策略日志，验证服务访问                 | “SELinux/AppArmor 是否启用？策略是否被篡改或禁用？”       |                                |
|               | 防护服务异常               | 防火墙、入侵防护（IDS/IPS）未启动             | 系统暴露风险，安全事件未阻断   | `systemctl status firewalld`, `ps -ef                          | grep snort`                          | 查看防护服务状态，确认是否生效及日志记录正常                    | “防护服务是否运行？日志是否正常？是否有未阻断的攻击事件？” |
| **账户滥用/异常行为** | 异常登录或账户被攻击           | 密码被猜测、暴力破解、僵尸账户                  | 登录失败或成功异常，系统告警   | `last`, `lastb`, `faillog`, `journalctl`                       | 分析登录历史和失败记录，确认异常来源和时间段               | “是否有异常登录？失败次数异常吗？来源 IP 是否可疑？”             |                                |
|               | 账户权限滥用               | 非授权用户提权或使用过期账户                   | 系统敏感操作异常或告警      | `sudo -l`, `id 用户`                                             | 核查 sudo 日志和账户状态，排查未授权操作              | “用户是否使用未授权权限操作？过期账户是否仍在使用？”               |                                |
| **内核/系统漏洞**   | 内核安全模块异常             | 内核漏洞、补丁未更新                       | 系统易受攻击，可能出现权限提升  | `uname -r`, 查看安全公告和补丁状态                                        | 检查内核版本和补丁历史，核对已知漏洞列表                 | “内核是否存在已知漏洞？补丁是否最新？安全模块是否启用？”             |                                |
|               | 内核模块加载异常             | 模块被禁止或损坏                         | 系统功能异常，安全功能不可用   | `lsmod`, `dmesg`                                               | 查看模块加载状态，检查内核日志异常                    | “关键内核模块是否加载？是否有报错或模块损坏？”                  |                                |